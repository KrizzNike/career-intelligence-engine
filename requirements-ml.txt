# =========================================================
# Career Intelligence Engine - ML / NLP / RAG dependencies
# Target Python: 3.11 - prebuilt wheels for EVERYTHING here,
#   including torch + sentence-transformers. No compiler needed.
# Install AFTER requirements.txt:
#   pip install -r requirements-ml.txt
#
# Heaviest packages: torch (~750MB), spacy model (~12MB),
#   chromadb deps. First install ~5-8 min on broadband.
# =========================================================

# ---- Core data ----
pandas==2.2.3
numpy==1.26.4

# ---- NLP ----
spacy==3.7.5
# NOTE: the spaCy language model is NOT a PyPI package.
# After this file installs, run separately:
#   python -m spacy download en_core_web_md
# (Medium model: entities PERSON, ORG, GPE, DATE + word vectors for Week 6 similarity.)

# ---- ML ----
scikit-learn==1.5.2
xgboost==2.1.1
imbalanced-learn==0.12.4

# ---- Embeddings / matching (Week 6 advanced, Week 9) ----
sentence-transformers==3.1.1
torch==2.4.1                 # CPU wheel; full ML stack works on 3.11

# ---- RAG (Week 10) ---- local Ollama + Chroma vector store
langchain==0.3.4
langchain-community==0.3.3
langchain-ollama==0.2.0
chromadb==0.5.15

# ---- Web app (Week 12) ----
streamlit==1.39.0
altair==5.4.1
