# =========================================================
# Career Intelligence Engine - Phase 0/1 core dependencies
# Pinned where a 3.14 wheel is known-good; unpinned where the
# pinned version lacks a 3.14 wheel. Pure-Python where possible.
#
# NOTE on Python 3.14 (user's choice, 2026-07-17):
#   - Several packages have NO prebuilt 3.14 wheel and require a
#     C++ build toolchain (VS Build Tools 2022) to compile.
#   - See requirements-ml.txt for the heavy ML/RAG stack and its
#     blockers (esp. torch has no 3.14 wheel at all).
# Install:
#   python -m venv .venv
#   .venv\Scripts\activate
#   pip install -r requirements.txt
# =========================================================

# ---- Database (MySQL 8) ---- all have 3.14 wheels
mysql-connector-python          # unpinned: 8.4.0 pin lacks 3.14 wheel
PyMySQL                         # pure-Python MySQL driver
SQLAlchemy==2.0.36

# ---- Document parsing (Week 4) ----
python-docx                     # .docx parsing, pure-Python
pdfplumber                      # PDF text extraction (replaces PyMuPDF on 3.14)
# PyMuPDF==1.24.10             # DEFERRED: no 3.14 wheel; needs VS Build Tools.
                                # If you still want MuPDF, install build tools
                                # then: pip install PyMuPDF==1.24.10

# ---- Config / env / logging / cli ----
python-dotenv==1.0.1
pydantic                        # unpinned for 3.14 wheel availability
pydantic-settings==2.6.0
loguru==0.7.2
rich==13.9.2
tqdm==4.66.5

# ---- Testing / quality ----
pytest==8.3.3
pytest-cov==5.0.0
ruff                            # unpinned; ships wheels for 3.14
mypy                            # unpinned
