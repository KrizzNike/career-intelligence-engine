# =========================================================
# Career Intelligence Engine - Phase 0/1 core dependencies
# Target Python: 3.11 (prebuilt wheels for the full stack;
#   no VS Build Tools required). See README §Setup.
# Install:
#   python -m venv .venv
#   .venv\Scripts\activate
#   pip install -r requirements.txt
# =========================================================

# ---- Database (MySQL 8) ----
mysql-connector-python==8.4.0
PyMySQL==1.1.1
SQLAlchemy==2.0.36

# ---- Document parsing (Week 4) ----
python-docx==1.1.2
pdfplumber==0.11.4

# ---- Config / env / logging / cli ----
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.0
loguru==0.7.2
rich==13.9.2
tqdm==4.66.5

# ---- Testing / quality ----
pytest==8.3.3
pytest-cov==5.0.0
ruff==0.7.4
mypy==1.13.0
